# Bytecode
__pycache__/
*.py[cod]

# Notebook checkpoints
.ipynb_checkpoints/

# Data is committed (small, ~360K) so the repo is self-contained on Colab.
# data/raw/ holds the frozen source files that let the pipeline run offline.

# Local test / tooling caches
.pytest_cache/
.ruff_cache/
*.egg-info/

# Anaconda notebook server workspace (local-only)
anaconda_projects/

# Transient run logs
*.log

# OS junk
.DS_Store
